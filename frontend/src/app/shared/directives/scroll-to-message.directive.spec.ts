import { ScrollToMessageDirective } from './scroll-to-message.directive';

describe('ScrollToMessageDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollToMessageDirective();
    expect(directive).toBeTruthy();
  });
});
