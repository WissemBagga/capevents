
server:
  port : 8080
spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/capevents
    username: capevents
    password: root

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

  flyway:
    enabled: true
    locations: classpath:db/migration
